public class NodeVector {
    Node node;
    int layer;
    boolean isLeft;
    public NodeVector(Node node, int layer, boolean isLeft){
        this.node = node;
        this.layer=layer;
        this.isLeft=isLeft;
    }
}
